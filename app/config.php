<?php
/**
 * Tracker_01 — конфигурация приложения (Кейс №1, MPV на PHP + MySQL).
 * Локальные учётные данные БД — только для разработки, НЕ для продакшена.
 */

const APP_NAME = 'Stopa';
const DB_HOST = '127.0.0.1';
const DB_PORT = '3306';
const DB_NAME = 'simple_tasks';
const DB_USER = 'root';
const DB_PASS = '';

const APP_DEBUG = true;

/** Включить вывод цветного приоритета и т.п. — без специфики БД. */